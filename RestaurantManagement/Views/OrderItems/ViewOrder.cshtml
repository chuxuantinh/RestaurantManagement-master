@model IEnumerable<RestaurantManagement.Models.OrderItem>

@{
    ViewBag.Title = "ViewOrder";
    Layout = "~/Views/Shared/ViewOrderLayout.cshtml";
}


@*@model IEnumerable<RestaurantManagement.Models.OrderItem>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Menu.Name)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Menu.Description)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.Quantity)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Menu.Price)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Menu.Name)
            </td>
            @*<td>
                @Html.EditorFor(modelItem => item.Quantity, new { @class = "form", @value = item.Quantity, htmlAttributes = new { style = "width: 35px" } })

            </td>*@
            @*<td>
                @Html.DisplayFor(modelItem => item.Menu.Description)
            </td>*@
            <td>
                @Html.DisplayFor(modelItem => item.Quantity)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Menu.Price)
            </td>

            <td>
                
                @Html.ActionLink("Add to Quantity", "AddToQuantity", "OrderItems", new { id = item.MenuId }, new { @class = "btn btn-secondary btn-xs" }) |
                @Html.ActionLink("Remove From Quantity", "RemoveFromQuantity", "OrderItems", new { id = item.MenuId }, new { @class = "btn btn-secondary btn-xs" }) |
                @Html.ActionLink("Back to Menu", "Create", "OrderItems", new { id = item.MenuId }, new { @class = "btn btn-secondary btn-xs" }) |
                @Html.ActionLink("Check Out", "CheckOut", "OrderItems", new { id = item.MenuId }, new { @class = "btn btn-secondary btn-xs" })

                @*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
    @Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
            </td>
        </tr>
    }

</table>